{% extends 'admin/base.html.twig' %}

{% block content_title %}
    {{ 'admin.job.create.title'|trans }}
{% endblock %}


{% block content %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-primary pull-right">
        {{ 'common.button.save'|trans }}
    </button>
    {{ form_end(form) }}
{% endblock %}
